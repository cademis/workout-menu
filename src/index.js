import {
  renderHeader,
  renderMainContent,
  renderMainContent2,
  renderFooter,
} from "./render";
import css from "./style.css";

renderHeader();

// renderMainContent();

renderMainContent2();

renderFooter();
